steps:
  - id: 'docker-build'
    name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - '${_DOCKERFILE}'
      - '-t'
      - '${_IMAGE}'
      - '${_CONTEXT}'

images:
  - '${_IMAGE}'

options:
  logging: CLOUD_LOGGING_ONLY
